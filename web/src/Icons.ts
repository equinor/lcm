// @ts-ignore
import { Icon } from '@equinor/eds-core-react'
// @ts-ignore
import { comment_important, edit, filter_alt, info_circle, refresh, add_box, save, play } from '@equinor/eds-icons'

Icon.add({
  play,
  filter_alt,
  info_circle,
  refresh,
  edit,
  comment_important,
  add_box,
  save,
})

export default Icon
